<view class="notification-panel">
  <block wx:if="{{notifications.length}}">
    <block wx:for="{{notifications}}" wx:key="id">
      <view 
        class="notification-item {{item.read ? 'read' : 'unread'}}" 
        data-notification-id="{{item.id}}" 
        bindtap="onTap"
      >
        <view class="notification-header">
          <view class="notification-icon">{{item.icon}}</view>
          <view class="notification-content">
            <view class="notification-title">{{item.typeText}}</view>
            <view class="notification-time">{{item.createdAtText}}</view>
          </view>
          <view class="notification-status">
            <view class="unread-dot" wx:if="{{!item.read}}"></view>
          </view>
        </view>
        <view class="notification-message" wx:if="{{item.message}}">
          <text>{{item.message}}</text>
        </view>
        <view class="notification-meta" wx:if="{{item.orderNo}}">
          <text class="notification-meta__label">订单编号</text>
          <text class="notification-meta__value">{{item.orderNo}}</text>
        </view>
        <view class="notification-meta" wx:if="{{item.statusText}}">
          <text class="notification-meta__label">当前状态</text>
          <text class="notification-meta__value">{{item.statusText}}</text>
        </view>
      </view>
    </block>
  </block>
  <view class="notification-empty" wx:else>
    <text>{{emptyText}}</text>
  </view>
</view>
